pytest==8.3.4
supabase==2.11.0
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
python-dotenv==1.0.1
PyJWT==2.10.1
cryptography==44.0.0

# Module 1: backend app, ingestion, chat
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic-settings==2.7.1
anthropic==0.44.0
voyageai==0.3.2
langsmith==0.10.4
pypdf==5.1.0
python-docx==1.1.2
beautifulsoup4==4.12.3
httpx==0.27.2

# Module 3a: MCP server (Streamable HTTP). sse-starlette is pinned below its
# 3.x line because 3.x requires starlette>=0.49, which conflicts with the
# starlette<0.42 that fastapi 0.115 pins.
mcp==1.28.1
sse-starlette==2.1.3

# Module 7: automations engine — cron expression validation (7a) + scheduling (7b).
croniter==6.0.0

# v1.2.0: the GoTo Connect notification channel is a WebSocket, so the bridge
# needs a client. (uvicorn[standard] already pulls this in transitively; pinning
# it here makes it a declared dependency rather than a lucky one.)
websockets==15.0.1
